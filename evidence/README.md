# Integration evidence

This directory contains versioned observations from real backend runs. Each
record identifies the runtime revision, backend source tree, model checkpoint,
input, environment, and transaction outcome.

These records show that one stated configuration ran successfully. They are not
performance benchmarks and do not establish behavior on other models, devices,
operating systems, or dependency versions.
